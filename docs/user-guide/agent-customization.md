# Customizing Lightfall with the Agent

A tutorial on asking the embedded agent to build or modify a UI panel from a natural-language request, with each change committed to the beamline plugin repository.

CONTENT-PENDING
